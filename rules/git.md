# Git Workflow

- Commit messages: imperative mood, <50 chars, no period
- Branch names: kebab-case (feature/add-auth, fix/login-bug)
- Always check `git status` before committing to know what you're staging
- For complex changes, create atomic commits per logical unit
