# Claude Code — Extensions

Extends: @../AGENTS.md

## Claude-Specific

- Don't mention Claude or AI in commit messages
- Use the Read/Edit/Write tools instead of cat/sed/echo in Bash
- Use Glob/Grep tools instead of find/grep in Bash
